/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['itihbhxqrpheeehfuvhf.supabase.co'], // Aggiungi il dominio di Supabase qui
      },
};

module.exports = nextConfig;
